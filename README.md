# Developer Profile

A simple developer profile page built with Vite and React as part of my React course.

## Setup

Clone the repository and install the dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Open the local URL shown in the terminal to view the profile page.
